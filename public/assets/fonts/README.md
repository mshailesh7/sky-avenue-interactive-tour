# Brand fonts

These files power the login / welcome / admin UI:

- `Classyvogueregular.ttf` → display / headings (`ClassyVogue`)
- `FuturaBT-Light.otf` → body / UI text (`FuturaBT`)

Served from `/assets/fonts/` and declared in `brand.css`.
